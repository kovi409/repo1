pipeline {
    agent any
    
    stages {
        stage("build") {
            steps {
                echo "building maven project"  
            }   
        }

        stage("package") {
            steps {
                echo "packing of maven"
            }
        }
    }
}
